events {
    worker_connections 1024;
}

http {
    upstream rate_limiter_nodes {
        # Round-robin distribution to nodes
        server app-node-1:8000;
        server app-node-2:8000;
        server app-node-3:8000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://rate_limiter_nodes;
            
            # Pass important headers through to the app nodes
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            
            # Specifically preserve custom headers
            proxy_set_header X-Customer-Id $http_x_customer_id;
            
            # Disable buffering for real-time latency
            proxy_buffering off;
        }
    }
}
